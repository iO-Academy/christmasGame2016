$(function() {
    $(document).keydown(function (e) {
        var levelNo
        var rand = Math.ceil(Math.random() * levelNo)
        console.log(rand)
        if (e.keyCode == 32) {
            // $("#selection" + )
        }
    })


})